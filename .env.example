# Desarrollo: proxy de Vite reenvía /api al backend local.
VITE_API_URL=/api

# Producción (Vercel): URL pública del API en Render, con sufijo /api (las rutas axios son /products, /orders, …).
# VITE_API_URL=https://tu-servicio.onrender.com/api
